# 传播算法升级路线图

## Summary
目标是在不一次性把项目复杂度拉满的前提下，把当前“规则网格 + 同质用户 + 简单热度反馈”的教学型模型，逐步升级为更拟真的短视频传播模拟器。

推荐分 3 个阶段推进：
1. 低改动：保留现有框架，只增强用户和流量机制
2. 中改动：把传播结构从网格升级为社交网络图
3. 高改动：引入时间过程和平台策略，做接近研究型的模型

默认优先级：
`异质用户 -> 双通道曝光 -> 图网络 -> 时间过程/Hawkes -> Agent-Based`

## Phase 1: 低改动升级
在现有 `config + model + visualize` 结构上演进，不改变项目的总体使用方式。

- 给用户加入异质性参数
  每个单元不再共享同一组传播概率，而是增加活跃度、兴趣匹配度、影响力、疲劳阈值等个体属性。
- 拆分曝光来源
  将“看到视频”拆成两部分：邻居传播曝光、平台推荐曝光；两者分别计算，再合成最终曝光概率。
- 细化状态链
  将 `VIEWED` 进一步拆分为“刷到但未停留”“停留观看”“互动”“分享”等更细的行为状态，至少把“观看”和“分享”中间增加一个互动层。
- 为中间阶段加入停留、流失和回落机制
  在状态链变细之后，不能只让用户单向高概率前进；应为 `EXPOSED`、`VIEWED`、`ENGAGED` 增加“停留在原状态”“回落为沉默/失活”“有限期后流失”等出口，避免最终转化率长期接近 100%。
- 区分“阶段新增”与“阶段累计”口径
  统计上应明确区分每一步新增人数、当前处于该状态的人数和全程累计进入过该状态的人数，避免摘要和漏斗指标因为口径混用而失真。
- 拆分社交传播深度与推荐触达深度
  当前传播深度容易被推荐链路掩盖；后续应分别记录“由社交链传播得到的层数”和“由平台推荐直接触达”的用户规模，用来判断到底是推荐过强还是社交链过浅。
- 参数配置化
  把当前硬编码的经验系数统一收口到配置层，避免模型逻辑里散落固定常数。
- 输出更完整的统计指标
  增加曝光量、观看转化率、互动率、分享率、传播深度、生命周期峰值时间等指标。
- 在 Phase 1 中优先验证漏斗是否真正分层
  这一阶段的验收不应只看程序能否运行和峰值是否下降，还应检查 `EXPOSED > VIEWED > ENGAGED > SHARING` 是否形成稳定层级、整体转化率是否显著低于 100%、以及传播深度是否不再长期停留在 1。

### Phase 1 机制调整草案

#### 1. 状态链草案

保留当前 6 状态结构：

- `UNSEEN`：未触达
- `EXPOSED`：刷到但未停留
- `VIEWED`：停留观看
- `ENGAGED`：已互动但未分享
- `SHARING`：正在传播
- `INACTIVE`：沉默或失活

不再把 `INACTIVE` 只理解为“传播衰退后进入”，而是统一作为本轮内容生命周期内的终止态，允许以下来源进入：

- `EXPOSED -> INACTIVE`
- `VIEWED -> INACTIVE`
- `ENGAGED -> INACTIVE`
- `SHARING -> INACTIVE`

这样 `INACTIVE` 同时覆盖“划过后流失”“看完但未再互动”“互动后未分享”“分享后失活”四类用户。

#### 2. 中间阶段转移草案

每个中间状态都拆成 3 类结果：

1. 前进到下一阶段
2. 留在当前阶段
3. 流失到 `INACTIVE`

具体规则建议如下：

- `UNSEEN -> EXPOSED`
  仍由社交曝光和平台推荐曝光共同决定。
- `EXPOSED -> VIEWED | EXPOSED | INACTIVE`
  观看概率受兴趣匹配、活跃度、热度影响；流失概率受低兴趣和低活跃度影响。
- `VIEWED -> ENGAGED | VIEWED | INACTIVE`
  互动概率受兴趣匹配、邻居传播氛围、热度影响；流失概率受疲劳和低社交响应倾向影响。
- `ENGAGED -> SHARING | ENGAGED | INACTIVE`
  分享概率受影响力、活跃度、邻居分享氛围影响；流失概率受疲劳和“只互动不转发”倾向影响。
- `SHARING -> SHARING | INACTIVE`
  保留当前传播衰退逻辑，但继续受传播时长和个人疲劳阈值控制。

所有中间阶段默认不允许回退到更早的正向状态，只允许“停留”或“流失”；如果后续要增强拟真度，再单独考虑 `ENGAGED -> VIEWED` 这类回落。

#### 3. 配置项草案

在现有配置基础上，新增一组显式流失参数和停留控制参数：

- `P_SKIP`
  `EXPOSED -> INACTIVE` 的基础概率
- `P_DROP_VIEW`
  `VIEWED -> INACTIVE` 的基础概率
- `P_DROP_ENGAGE`
  `ENGAGED -> INACTIVE` 的基础概率
- `HEAT_PROTECT_VIEW`
  热度对观看留存的保护作用
- `HEAT_PROTECT_ENGAGE`
  热度对互动留存的保护作用
- `INTEREST_DROP_WEIGHT`
  兴趣不足对中间流失的影响权重
- `ACTIVITY_DROP_WEIGHT`
  活跃度不足对中间流失的影响权重

现有配置中与前进相关的参数继续保留，但要求实现时把“前进概率”和“流失概率”分开计算，不能再让全部剩余概率默认变成“停留”。

#### 4. 统计口径草案

统计分成 3 层，避免继续混淆：

第一层：当前存量

- `exposed`
- `viewed`
- `engaged`
- `sharing`
- `inactive`

第二层：每步新增流量

- `new_exposures`
- `new_views`
- `new_engagements`
- `new_shares`
- `new_skips`
- `new_drop_view`
- `new_drop_engage`
- `new_inactive_from_sharing`

第三层：累计口径

- `cumulative_exposures`
- `cumulative_views`
- `cumulative_engagements`
- `cumulative_shares`
- `cumulative_inactive`

摘要输出只使用累计口径或全程聚合口径，不使用最后一步口径。

#### 5. 漏斗指标草案

在当前阶段转化率基础上，新增全程漏斗指标：

- `overall_view_conversion = cumulative_views / cumulative_exposures`
- `overall_engagement_conversion = cumulative_engagements / cumulative_views`
- `overall_share_conversion = cumulative_shares / cumulative_engagements`
- `skip_rate = cumulative_skip / cumulative_exposures`
- `view_drop_rate = cumulative_drop_view / cumulative_views`
- `engage_drop_rate = cumulative_drop_engage / cumulative_engagements`

验收时优先看全程指标，不以最后一步的阶段转化率作为主要判断依据。

#### 6. 传播深度草案

将深度拆成两套：

- `social_depth`
  用户第一次通过社交链触达时的传播层数
- `recommended_reach`
  用户第一次通过推荐链触达时的累计人数

统计规则：

- 如果用户首次通过社交链触达，则 `social_depth = parent_depth + 1`
- 如果用户首次通过推荐链触达，则不增加社交深度，只增加推荐触达计数
- 如果用户同时满足两种曝光，优先记为社交触达，并额外记入“双重曝光”统计

摘要中“传播深度”默认指 `max(social_depth)`，不再混用推荐触达结果。

#### 7. 可视化草案

在当前 6 图布局基础上，优先替换或新增两类图：

- 阶段流失图
  展示 `new_skips`、`new_drop_view`、`new_drop_engage`、`new_inactive_from_sharing`
- 漏斗累计图
  展示 `cumulative_exposures`、`cumulative_views`、`cumulative_engagements`、`cumulative_shares`

如果版面不够，优先保留：

1. 状态存量图
2. 累计漏斗图
3. 阶段流失图
4. 社交深度/推荐触达图

#### 8. 验收标准草案

本轮机制调整完成后，以以下标准判断是否达到目标：

- 程序仍保持当前使用方式不变，入口和可视化调用方式不变
- 总人口守恒
- `overall_view_conversion < 1.0`
- `overall_engagement_conversion < 1.0`
- `overall_share_conversion < 1.0`
- `skip_rate`、`view_drop_rate`、`engage_drop_rate` 至少有一类明显大于 0
- `EXPOSED > VIEWED > ENGAGED > SHARING` 在主要传播阶段基本成立
- `max(social_depth)` 不再长期固定为 1，或至少能和 `recommended_reach` 分开解释

如果以上条件在多组参数下仍无法满足，则判定现有网格扩散机制本身不足，应提前进入更高优先级的结构升级。

### Phase 1 传播节奏放缓草案

当前实现虽然已经形成了漏斗分层，但从状态曲线看，系统仍然在较早步数内快速进入稳定区。为解决“传播过程趋于稳定过快”的问题，建议分 3 档推进，优先从低成本手段开始。

#### A. 只调参数方案

目标：

- 拉长前中期传播时间
- 避免曲线在前几十步就基本变平
- 不改变当前函数接口和状态定义

具体建议：

1. 降低初始传播源数量
   将 `INITIAL_SHARERS` 作为第一优先级调节项。优先尝试从当前值下调到更低区间，使扩散从更小的局部种子开始。

2. 延长传播者存活时间
   适度提高 `MAX_SHARING_STEPS`，同时不要同步提高分享强度，让传播者“传播得更久”，而不是“传播得更猛”。

3. 下调早期推荐速度
   进一步压低 `P_RECOMMEND` 和 `HEAT_BOOST_RECOMMEND`，避免推荐链路过早完成全域触达。

4. 降低中间阶段即时决策强度
   在不改变机制的前提下，适度同时下调：
   - `P_VIEW`
   - `P_ENGAGE`
   - `P_SHARE`
   - `P_SKIP`
   - `P_DROP_VIEW`
   - `P_DROP_ENGAGE`

   原则不是让流失或前进更极端，而是让更多用户在中间状态停留更久。

验收标准：

- 峰值不应过早出现
- 主要状态曲线在更长时间区间内保持变化
- 不重新退回“整体转化率接近 100%”

#### B. 小改机制方案

目标：

- 在不改整体状态链的前提下，引入显式等待时间
- 让每个中间状态至少停留若干步后才允许前进或流失

具体草案：

1. 为 `EXPOSED`、`VIEWED`、`ENGAGED` 增加阶段停留计时器
   新增与 `sharing_time` 类似的数组：
   - `exposed_time`
   - `viewed_time`
   - `engaged_time`

2. 增加最短停留步数参数
   建议新增：
   - `MIN_EXPOSED_STEPS`
   - `MIN_VIEWED_STEPS`
   - `MIN_ENGAGED_STEPS`

3. 转移门控规则
   在达到最短停留步数前：
   - 用户不能前进到下一阶段
   - 也不能进入阶段流失
   - 只能继续停留在当前阶段

4. 达到最短停留步数后，再应用现有三分机制
   这样当前概率公式可以保留，但真正执行时会被时间门控延后。

预期收益：

- 曲线展开时间会明显增加
- 中间状态更容易形成可观察的平台期
- 不需要推翻现有统计口径和可视化结构

#### C. 中改机制方案

目标：

- 不只延缓单步转移，而是让用户转化依赖“累积接触”或“累积兴趣”
- 让系统更像真实平台中的延迟决策过程

具体草案：

1. 引入累积意愿分数
   为每个用户增加：
   - `watch_score`
   - `engage_score`
   - `share_score`

2. 分数按步累积
   累积来源包括：
   - 邻居传播影响
   - 热度
   - 兴趣匹配
   - 活跃度

3. 阈值触发转移
   当对应分数超过阈值时，用户才进入下一阶段。建议新增：
   - `WATCH_THRESHOLD`
   - `ENGAGE_THRESHOLD`
   - `SHARE_THRESHOLD`

4. 分数可衰减
   若用户连续几步未继续受到强化，分数可按衰减系数回落，避免所有人迟早都必然跨阈值。

预期收益：

- 传播节奏显著放慢
- 热点会更像“逐步发酵”，而不是立即爆发后迅速稳定
- 更容易形成局部积压和阶段滞后

代价：

- 模型状态量明显增加
- 需要新增一组分数数组和阈值配置
- 统计解释复杂度上升

#### 推荐执行顺序

1. 先做 A 档
   低成本观察当前机制能否仅靠节奏调整得到更长传播周期。
2. 如果 A 档无明显改善，再做 B 档
   这是最推荐的下一步，因为它兼顾低风险和高收益。
3. 如果 B 档仍不足以拉长节奏，再考虑 C 档
   这一步已经接近“机制升级”，不建议和图网络改造同时进行。

#### 本项验收标准

无论采用 A、B、C 哪一档，最终至少应满足以下之一：

- 峰值热度和峰值传播人数明显晚于当前版本出现
- `EXPOSED`、`VIEWED`、`ENGAGED` 中至少两层出现更长时间的平台区
- 在总步数不变的情况下，状态曲线在前 20% 步数内不再基本完成主要变化

如果上述目标在 B 档后仍无法实现，则说明“稳定过快”问题已经超出当前规则网格结构的可调范围，应提前提升图网络改造优先级。

预期收益：
- 模型仍然简单可控
- 传播行为会明显比当前更像真实平台
- 适合作为后续升级的稳定基线

## Phase 2: 中改动升级
把传播拓扑从“二维规则邻居”改成“真实社交图”。

- 用图结构替代网格
  节点代表用户，边代表社交关系；优先考虑小世界网络或无标度网络。
- 区分强连接和弱连接
  不同边赋予不同传播权重，模拟熟人群传播和跨圈扩散。
- 保留平台推荐作为独立通道
  图传播负责社交扩散，推荐负责平台分发，两者并行。
- 引入社区结构
  将用户划分为不同兴趣圈层，内容在圈内传播更强，跨圈传播更难。
- 可视化从“网格动画”升级为“传播曲线 + 网络统计”
  重点展示不同社区触达、核心节点贡献、传播层数和峰值位置，而不再强依赖二维动画。

预期收益：
- 更接近真实短视频传播网络
- 能自然出现头部节点、局部爆发、跨圈跳跃传播
- 为后续研究平台机制打下基础

## Phase 3: 高改动升级
把简单概率推进升级为更真实的时间事件过程和决策过程。

- 引入阈值传播模型
  用户不是独立随机被触发，而是在足够多邻居影响后才进入传播状态，适合模拟从众效应。
- 引入 Hawkes 自激点过程
  每次分享都会短时抬高后续分享概率，影响再随时间衰减，适合模拟热点爆发和冷却。
- 引入生存分析/危险率机制
  把“多久后分享”“多久后流失”建成时间相关事件，而不是每一步固定概率。
- 引入平台策略反馈
  推荐分发不只依赖总热度，而依赖完播、互动、转发、衰减速度等综合表现。
- 升级为 Agent-Based Model
  每个用户具备独立属性、决策规则和行为历史，平台按内容表现动态分发。

预期收益：
- 拟真度最高
- 能模拟短视频热点的爆发、衰退、再激活
- 更适合做研究展示或论文型分析
- 实现和调参成本最高

## Implementation Changes
建议按下面顺序落地，避免一次性重写：

1. 第一轮
   保留网格结构，只加异质用户、双通道曝光、细化状态和统计指标。
1.5 第二轮
   在不改网格拓扑的前提下，为中间状态加入停留/流失/回落机制，并拆分推荐触达与社交传播深度；这一步优先于图网络升级，因为当前模型的主要瓶颈已从参数不足转为机制过于单向。
   交付内容应包括：新的中间流失参数、统一的新增/累计统计口径、更新后的摘要输出、至少一张专门展示流失和漏斗累计的图。
2. 第二轮
   抽象“邻接关系”接口，把网格邻域计算替换成通用图邻接计算。
3. 第三轮
   把“热度递推”抽象成独立模块，允许切换为阈值模型或 Hawkes 模型。
4. 第四轮
   再决定是否进入完整 Agent-Based 版本；这一步应视项目目标决定，不默认纳入 v1。

需要提前稳定的公共接口：
- 输入：配置对象应明确区分用户参数、平台参数、网络参数、传播参数
- 输出：统一返回逐步统计、节点状态历史、传播事件日志
- 可视化：从依赖二维网格，改为依赖通用统计结果

## Test Plan
每个阶段都应补同一类验证，避免模型升级后失控。

- 固定随机种子下结果可复现
- 用户总量守恒，状态转移合法
- 曝光、互动、分享、流失的计数口径一致
- 平台推荐通道关闭时，模型应退化为纯社交传播
- 社交传播关闭时，模型应退化为纯推荐扩散
- 图网络下，高度节点的传播贡献应显著高于普通节点
- Hawkes/阈值模型下，应能出现比当前模型更明显的爆发峰和衰减尾部

## Assumptions
- 目标是“更拟真”，不是“最省计算量”。
- 项目仍以教学/实验为主，不优先接入真实平台数据。
- 默认建议先做低改动版本，不直接跳到 Agent-Based。
- 如果后续要继续细化，下一步最值得产出的是一份“Phase 1 的详细设计稿”，把新增状态、参数和公式先定死。
